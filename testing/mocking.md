# What is a mock object in testing?
- A simulated object that mimics the behavior of real objects x
- A real database connection
- A type of test framework
- A production component

# What is the difference between a stub and a mock?
- Stubs provide predefined responses, mocks verify interactions x
- They are the same thing
- Stubs are faster than mocks
- Mocks are only for databases

# Which of the following are types of test doubles?
- Mocks x
- Stubs x
- Fakes x
- Compilers

# Which are benefits of using test doubles?
- Faster test execution x
- Better test isolation x
- Reduced dependencies on external systems x
- Automatic bug fixing

# Evaluate these statements about mocking:
- Mocks help isolate the unit being tested t
- Mocking is only used in integration testing f
- Mocks can simulate complex behavior t
